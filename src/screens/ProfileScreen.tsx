import React from 'react';
import ProfileScreenForm from '../screenForms/ProfileScreenForm';

type Props = {};

const ProfileScreen = (props: Props) => {
  return <ProfileScreenForm />;
};

export default ProfileScreen;
