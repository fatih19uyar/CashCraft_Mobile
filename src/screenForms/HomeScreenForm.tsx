import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const HomeScreenForm = (props: Props) => {
  return (
    <View>
      <Text>HomeScreenForm</Text>
    </View>
  );
};

export default HomeScreenForm;

const styles = StyleSheet.create({});
