import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

const NewReservationForm = (props: Props) => {
  return (
    <View>
      <Text>NewReservationForm</Text>
    </View>
  );
};

export default NewReservationForm;
